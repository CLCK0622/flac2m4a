# Dependencies
# This directory contains all of the project's dependencies and can be very large.
# It should always be ignored and regenerated by running `pnpm install`.
/node_modules

# Logs
# These files are created by npm or pnpm when errors occur.
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated output files
# Ignore the .m4a files that are the output of this script.
*.m4a

# Operating System generated files
# These files are created by macOS, Windows, and Linux systems and are not part of the source code.
.DS_Store
Thumbs.db
._*

# IDE and Editor directories
# These folders contain user-specific settings for code editors like WebStorm, VS Code, etc.
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
# This file often contains sensitive keys and should never be committed.
.env
.env.local
.env.*.local

# PNPM Configuration file
# This file is often specific to the local setup, especially if it contains credentials or local paths.
# However, if it only contains project-wide settings (like the ffmpeg mirror), you might choose to commit it.
# Ignoring it by default is a safe choice.
.npmrc

